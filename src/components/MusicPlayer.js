import React from 'react';

const MusicPlayer = props => {
    return (
        <div className='music-player' key={props.id}>
        <h1>Operation Titan Music Player</h1>
        </div>
    )
}

export default MusicPlayer;